[Unit]
Description=Discord Rich Presence: Claude Code CLI dan lagu yang diputar
Documentation=https://github.com/Lamiore/vibe-coding-presence
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/workspace/projects/lagi-ngapain/cc_daemon.py
# Kode 2 = client_id belum diisi. Itu salah konfigurasi, bukan kegagalan;
# mengulangnya tiap 10 detik cuma membanjiri journal tanpa memperbaiki apa pun.
SuccessExitStatus=2
Restart=on-failure
RestartSec=10
# Daemon cuma bicara ke soket di XDG_RUNTIME_DIR dan membaca satu berkas
# konfigurasi -- tidak ada alasan dia bisa menulis ke mana-mana.
NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=%t
# Satu-satunya tempat tulis di luar %t: singgahan sampul album. Tanpa baris
# ini ~/.cache ikut read-only, dan cc_sampul menelan galat tulisnya diam-diam
# -- jadi iTunes ditanya ulang tiap lagu, tiap kali daemon dinyalakan.
CacheDirectory=lagi-ngapain

[Install]
WantedBy=default.target
